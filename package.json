{
  "name": "mydealz-manager",
  "version": "2.0.0",
  "private": true,
  "description": "MV3 Browser-Extension für mydealz.de — kein npm-Runtime-Deps, nur Tests + Build via plain Node.",
  "scripts": {
    "build": "node build.js",
    "test": "node tests/run-tests.js"
  }
}
